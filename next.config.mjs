/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['eventbucket22.s3.ca-central-1.amazonaws.com'], // Add your allowed domains here
    },
  };
  
  export default nextConfig;
  